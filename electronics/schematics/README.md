# Schematics

Circuit diagrams.

Upload:
- KiCad source (`.kicad_sch`, `.kicad_pro`)
- PDF export of the same schematic (so people without KiCad can read it)

Keep one schematic per major board. For this chair the boards are:

- `main-controller.kicad_sch` — ESP32-S3 + I²C bus + pump/valve drivers + LED ring header
- `power-board.kicad_sch` — TP4056 charging + boost converter + battery protection

> **Status: to be updated** — no schematics uploaded yet.
